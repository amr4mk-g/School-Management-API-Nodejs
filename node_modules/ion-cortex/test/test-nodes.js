
const Cortex         = require('../index');

/* -- */
const cortex = new Cortex({
    prefix: "spacejat",
    url: "redis://localhost:6379",
    type: 'm1',
    state: ()=>{
        let time = new Date().getTime();
        return { sendTime: time}; 
    }
});

let last = {};


setInterval(()=>{
    console.log('_');
    console.log(Object.keys(cortex.nodes));
    console.log(JSON.stringify(cortex.nodes));
    // if(cortex.nodes.sfu){
    //     let got  = JSON.stringify(cortex.nodes);
    //     if(got.length == last.length){
    //         console.log(`no change got: ${got.length}`)
    //     } else {
    //         console.log(`changed: ${got.length}`);
    //     }
    //     console.log(got);
    //     last = got;
    // }
}, 1000)